{
  "name": "claude-swarm-lite",
  "version": "0.2.0",
  "private": true,
  "description": "Minimal Electron GUI that runs several Claude Code sessions in tabs by spawning your local `claude` CLI inside a login shell. It never touches OAuth tokens — each tab is a real `claude` process that inherits the auth you already set up, so everything runs on your normal subscription limits.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "node test/themes.test.js && node test/preload-contract.test.js",
    "dist": "electron-builder --mac",
    "dist:win": "electron-builder --win",
    "release": "node scripts/release.mjs"
  },
  "license": "MIT",
  "build": {
    "appId": "io.swarm.claude-swarm-lite",
    "productName": "Claude Swarm Lite",
    "npmRebuild": false,
    "artifactName": "claude-swarm-lite-${version}-${arch}.${ext}",
    "files": [
      "main.js",
      "git.js",
      "preload.js",
      "renderer/**/*",
      "package.json"
    ],
    "asarUnpack": [
      "**/node_modules/@homebridge/node-pty-prebuilt-multiarch/**"
    ],
    "afterPack": "build/afterPack.cjs",
    "mac": {
      "target": "dmg",
      "category": "public.app-category.developer-tools",
      "identity": null
    },
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true
    },
    "directories": {
      "output": "dist"
    }
  },
  "dependencies": {
    "@homebridge/node-pty-prebuilt-multiarch": "^0.13.1",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/headless": "^5.5.0",
    "@xterm/xterm": "^5.5.0"
  },
  "devDependencies": {
    "electron": "29.4.6",
    "electron-builder": "^26.15.3"
  },
  "overrides": {
    "@noble/hashes": "1.8.0"
  }
}
